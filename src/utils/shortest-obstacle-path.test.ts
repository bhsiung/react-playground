import {getPath } from './shortest-obstacle-path'

describe('test getPath', () => {
  it('works', () => {
    const pathMap 
    expect(breakPattern('abc')).toEqual([
      { value: 'a', hasStar: false },
      { value: 'b', hasStar: false },
      { value: 'c', hasStar: false },
    ])

